@model List<ResultProductDto>
@{
	ViewData["Title"] = "Index";
	Layout = "~/Views/AdminLayout/Index.cshtml";
	int count = 0;
}

<div class="content">
	<div class="container-fluid">
		<h4 class="page-title">Tables</h4>
		<div class="row">
			<div class="col-md-12">
				<div class="card">
					<div class="card-header">
						<div class="card-title">Product List</div>
					</div>
					<div class="card-body">
						<div class="card-sub">
							You can perform product-related operations below.
						</div>
						<table class="table mt-3">
							<thead>
								<tr>
									<th scope="col">#</th>
									<th scope="col">Name</th>
									<th scope="col">Status</th>
									<th scope="col">Description</th>
									<th scope="col">Price</th>
									<th scope="col">ImageUrl</th>
									<th scope="col">Delete</th>
									<th scope="col">Update</th>
								</tr>
							</thead>
							<tbody>
								@foreach (var item in Model)
								{
									count++;
									<tr>
										<td>@count</td>
										<td>@item.Name</td>
										<td>@item.Status</td>
										<td>@item.Description</td>
										<td>@item.Price</td>
										<td>@item.ImageUrl</td>
										<td><a href="/AdminProduct/Delete/@item.Id" class="btn btn-outline-danger">Delete</a></td>
										<td><a href="/AdminProduct/Update/@item.Id" class="btn btn-outline-success">Update</a></td>
									</tr>
								}
							</tbody>
						</table>
						<a href="/AdminProduct/Create" class="btn btn-outline-primary">Add Product</a>
					</div>
				</div>

			</div>

		</div>
	</div>
</div>


